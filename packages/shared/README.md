# Shared Package

Shared TypeScript contracts for API DTOs, Socket.IO events, role permissions, and cross-application validation schemas.

This package will be filled as the API and frontend modules are implemented.
